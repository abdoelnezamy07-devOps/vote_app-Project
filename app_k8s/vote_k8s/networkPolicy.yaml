apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vote-networkpolicy
  namespace: security
spec:
  podSelector:
    matchLabels:
      app: vote
      tier: frontend
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: ingress-nginx 
    ports:
    - protocol: TCP
      port: vote-port
  egress:
  - to:
      - podSelector:
          matchLabels:
            app: redis 
            tier: backend
    ports:
    - protocol: TCP
      port: c-redis-port
  - to:
      - namespaceSelector: {}
    ports:
     - protocol: UDP
       port: 53
     - protocol: TCP
       port: 53 
