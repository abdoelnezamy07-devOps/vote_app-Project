--- 
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: security
spec:
  type: ClusterIP
  selector:
    app: redis
    tier: backend
  ports:
    - port: 6379
      name: redis-svc-port
      targetPort: c-redis-port
